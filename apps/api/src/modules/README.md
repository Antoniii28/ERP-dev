# Módulos base

Esta carpeta contiene la estructura base de módulos del ERP.

Cada módulo debe tener una organización mínima con:

- `controllers/`
- `services/`
- `routes/`
- `types/`
- `index.ts`

La implementación funcional de cada módulo se realizará en fases posteriores.
